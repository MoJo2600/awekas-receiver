apiVersion: v1
kind: Service
metadata:
  name: {{ include "awekas-receiver.fullname" . }}
  labels:
    {{- include "awekas-receiver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awekas-receiver.selectorLabels" . | nindent 4 }}
